@* @model IEnumerable<DisplayProductDTO> *@
@{
    ViewBag.PageTitle = "Products Management";
    ViewBag.CardTitle = "View Products";
    Layout = "~/Views/Shared/_Dashboard.cshtml";
}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
<link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css" />



<div class="custom-card">

    <div class="custom-card-header">
        <h4><i class="fa-solid fa-box-open me-2 text-white"></i> Products List</h4>
    </div>

    <div class="d-flex justify-content-end mb-3">
        <a asp-controller="Product"
           asp-action="Create"
           class="btn btn-primary">
            <i class="fa-solid fa-circle-plus me-1"></i>
            Create Product
        </a>
    </div>

    <table id="mytable" class="table-custom display" width="100%">
        <thead>
            <tr>
                <th>Name</th>
                <th>Description</th>
                <th>Price</th>
                <th>Category</th>
                <th style="width:130px;">Actions</th>
            </tr>
        </thead>
        @* <tbody>
            @foreach(var product in Model)
            {
                <tr>
                    <td>@product.Name</td>
                    <td>@product.Description</td>
                    <td>@product.Price.ToString("C")</td>
                    <td>@product.CategoryName</td>
                    <td>
                        <a asp-controller="Product"
                           asp-action="Edit"
                           asp-route-id="@product.Id"
                           class="btn btn-success action-btn me-1">
                            <i class="fa-solid fa-pen-to-square"></i>
                        </a>
                        <button class="btn btn-danger action-btn delete-btn"
                                data-id="@product.Id">
                            <i class="fa-solid fa-trash"></i>
                        </button>
                    </td>
                </tr>
            }
        </tbody> *@
    </table>

</div>

@section Scripts {
    <script src="~/js/products.js"></script>
}